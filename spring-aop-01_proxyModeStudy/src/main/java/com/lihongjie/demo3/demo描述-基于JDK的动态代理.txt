1、本demo用的是基于JDK的动态代理技术（基于接口实现和反射技术的实现）。

2、被代理类(目标对象，如A类)，必须要有实现接口。因为基于JDK的动态代理技术本质上代理的是接口（类比静态代理）

3、基于JDK的动态代理，其原理本质上也是通过实现【被代理对象实现的接口】来生产代理对象的，且调用代理对象的代理方法时，会去调用
InvocationHandler中的invoke方法（说白了就是增强的代理方法。）ps：和静态代理的思想简直一毛一样。只是动态代理更加通用，且
不需要我们自己书写代理类的代码（通过实现重写InvoicationHandler帮我们自动生成）。